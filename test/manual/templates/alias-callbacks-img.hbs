{{> top title="Callbacks image, alias"}}

	<button type="button" class="array-done">Load image file, callback as argument (param as array)</button>
	<button type="button" class="array-done-optcb">Load image file, callback in object (param as array)</button>
	<button type="button" class="array-done-optargdfdcb">Load image file, callback in object, as argument and deferred (param as array)</button>

	<button type="button" class="array-done-recache">Load image file, no cache, callback as argument (param as array)</button>
	<button type="button" class="array-done-recache-optcb">Load image file, no cache, callback in object (param as array)</button>
	<button type="button" class="array-done-recache-optargdfdcb">Load image file, no cache, callback in object, as argument and deferred (param as array)</button>

	<button type="button" class="array-fail">Load non-existent image file, callback as argument (param as array)</button>
	<button type="button" class="array-fail-optcb">Load non-existent image file, callback in object (param as array)</button>
	<button type="button" class="array-fail-optargdfdcb">Load non-existent image file, callback in object and deferred (param as array)</button>

	<button type="button" class="array-fail-recache">Load non-existent image file, no cache, callback as argument (param as array)</button>
	<button type="button" class="array-fail-recache-optcb">Load non-existent image file, no cache, callback in object (param as array)</button>
	<button type="button" class="array-fail-recache-optargdfdcb">Load non-existent image file, no cache, callback in object and deferred (param as array)</button>

	<div class="sandbox"></div>

	<script>

		$('.array-done').on('click', function () {

			$.kist.loader
				.loadImage(['assets/img/test-01.jpg'], function ( img ) {
					console.log( 'Done' );
					console.log( arguments );
					$(img[0]).appendTo('.sandbox');
				});

		});

		$('.array-done-optcb').on('click', function () {

			$.kist.loader
				.loadImage({
					url: ['assets/img/test-01.jpg'],
					success: function ( img ) {
						console.log( 'Done' );
						console.log( arguments );
						$(img[0]).appendTo('.sandbox');
					}
				});

		});

		$('.array-done-optargdfdcb').on('click', function () {

			$.kist.loader
				.loadImage({
					url: ['assets/img/test-01.jpg'],
					success: function ( img ) {
						console.log( 'Done' );
						console.log( arguments );
						$(img[0]).appendTo('.sandbox');
					}
				}, function ( img ) {
					console.log( 'Done' );
					console.log( arguments );
					$(img[0]).appendTo('.sandbox');
				})
				.done(function ( img ) {
					console.log( 'Done' );
					console.log( arguments );
					$(img[0]).appendTo('.sandbox');
				});

		});

		$('.array-done-recache').on('click', function () {

			$.kist.loader
				.loadImage({
					url: ['assets/img/test-01.jpg'],
					cache: false
				}, function ( img ) {
					console.log( 'Done' );
					console.log( arguments );
					$(img[0]).appendTo('.sandbox');
				});

		});

		$('.array-done-recache-optcb').on('click', function () {

			$.kist.loader
				.loadImage({
					url: ['assets/img/test-01.jpg'],
					cache: false,
					success: function ( img ) {
						console.log( 'Done' );
						console.log( arguments );
						$(img[0]).appendTo('.sandbox');
					}
				});

		});

		$('.array-done-recache-optargdfdcb').on('click', function () {

			$.kist.loader
				.loadImage({
					url: ['assets/img/test-01.jpg'],
					cache: false,
					success: function ( img ) {
						console.log( 'Done' );
						console.log( arguments );
						$(img[0]).appendTo('.sandbox');
					}
				}, function ( img ) {
					console.log( 'Done' );
					console.log( arguments );
					$(img[0]).appendTo('.sandbox');
				})
				.done(function ( img ) {
					console.log( 'Done' );
					console.log( arguments );
					$(img[0]).appendTo('.sandbox');
				});

		});

		$('.array-fail').on('click', function () {

			$.kist.loader
				.loadImage(['assets/img/x.jpg'], function ( img ) {
					console.log( 'Done' );
					console.log( arguments );
					$(img[0]).appendTo('.sandbox');
				})
				.fail(function ( img ) {
					console.log( 'Fail' );
					console.log( arguments );
				});

		});

		$('.array-fail-optcb').on('click', function () {

			$.kist.loader
				.loadImage({
					url: ['assets/img/x.jpg'],
					success: function ( img ) {
						console.log( 'Done' );
						console.log( arguments );
						$(img[0]).appendTo('.sandbox');
					},
					error: function ( img ) {
						console.log( 'Fail' );
						console.log( arguments );
					}
				});

		});

		$('.array-fail-optargdfdcb').on('click', function () {

			$.kist.loader
				.loadImage({
					url: ['assets/img/x.jpg'],
					success: function ( img ) {
						console.log( 'Done' );
						console.log( arguments );
						$(img[0]).appendTo('.sandbox');
					},
					error: function ( img ) {
						console.log( 'Fail' );
						console.log( arguments );
					}
				})
				.fail(function ( img ) {
					console.log( 'Fail' );
					console.log( arguments );
				});

		});

		$('.array-fail-recache').on('click', function () {

			$.kist.loader
				.loadImage({
					url: ['assets/img/x.jpg'],
					cache: false
				}, function ( img ) {
					console.log( 'Done' );
					console.log( arguments );
					$(img[0]).appendTo('.sandbox');
				})
				.fail(function ( img ) {
					console.log( 'Fail' );
					console.log( arguments );
				});

		});

		$('.array-fail-recache-optcb').on('click', function () {

			$.kist.loader
				.loadImage({
					url: ['assets/img/x.jpg'],
					cache: false,
					success: function ( img ) {
						console.log( 'Done' );
						console.log( arguments );
						$(img[0]).appendTo('.sandbox');
					},
					error: function ( img ) {
						console.log( 'Fail' );
						console.log( arguments );
					}
				});

		});

		$('.array-fail-recache-optargdfdcb').on('click', function () {

			$.kist.loader
				.loadImage({
					url: ['assets/img/x.jpg'],
					cache: false,
					success: function ( img ) {
						console.log( 'Done' );
						console.log( arguments );
						$(img[0]).appendTo('.sandbox');
					},
					error: function ( img ) {
						console.log( 'Fail' );
						console.log( arguments );
					}
				})
				.fail(function ( img ) {
					console.log( 'Fail' );
					console.log( arguments );
				});

		});

	</script>

{{> bottom}}
